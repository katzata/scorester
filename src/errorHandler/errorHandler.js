const errorTypes = {
    fetch: {
        json: {
            userMessage: "Something went wrong while fetching a file! Please try again later.",
            logMessage: "Something went wrong while fetching $s1.json"
        }
    }
};

export default function errorHandler({ type }) {
    
};